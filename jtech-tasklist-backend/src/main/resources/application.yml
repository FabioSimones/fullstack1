spring:
  application:
    name: tasklist

  datasource:
    url: jdbc:postgresql://localhost:5432/task_database
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
